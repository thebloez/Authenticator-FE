package com.lab.fe.authenticator.repository;

import com.lab.fe.authenticator.model.Rc;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RcRepos extends JpaRepository<Rc,String> {
}
